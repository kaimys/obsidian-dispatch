# Obsidian resolves a plugin release by the exact version string in
# manifest.json, so the git tag must be "0.2.3", not "v0.2.3". npm's default
# tag-version-prefix is "v", which would silently produce a tag the community
# directory and BRAT cannot match.
tag-version-prefix=""
