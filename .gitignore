# build output
main.js
*.map

# dependencies
node_modules/

# per-vault runtime files — these live next to the plugin inside a vault,
# never in this repo. local.json is machine-specific by design.
data.json
local.json

# OS noise
.DS_Store
Thumbs.db

# the wiki, linked in as dispatch/wiki — the vault itself lives outside this repo
/dispatch/wiki
# device-local Codex preferences (sandbox/approval policy) — never shared
.codex/config.toml
