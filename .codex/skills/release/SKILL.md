---
name: release
description: Ship a version — test pass, bump, release note generated from the board, tag, promote the shipped tickets, announce.
---

# release

Every step of this workflow lives in one file, shared by all agents:
**`dispatch/workflow/release.md`**. Read it now and follow it exactly — this file
carries no steps of its own.

Wherever it says `<ARGS>`, substitute the arguments you were
invoked with (`[version]`).
