---
name: implementation-plan
description: Write the implementation plan for a refined ticket — stored in the ticket as the review surface — then extract durable decisions into ADRs.
---

# implementation-plan

Every step of this workflow lives in one file, shared by all agents:
**`dispatch/workflow/implementation-plan.md`**. Read it now and follow it exactly — this file
carries no steps of its own.

Wherever it says `<ARGS>`, substitute the arguments you were
invoked with (`<ticket id>`).
