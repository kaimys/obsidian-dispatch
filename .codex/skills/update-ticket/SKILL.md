---
name: update-ticket
description: Fold everything that arrived since the last edit — inline comments, thread replies, tracker comments, code drift — back into a ticket spec.
---

# update-ticket

Every step of this workflow lives in one file, shared by all agents:
**`dispatch/workflow/update-ticket.md`**. Read it now and follow it exactly — this file
carries no steps of its own.

Wherever it says `<ARGS>`, substitute the arguments you were
invoked with (`<ticket id>`).
