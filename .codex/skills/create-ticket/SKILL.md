---
name: create-ticket
description: Create a new ticket from a short description — duplicate check, spec from the template with full frontmatter, and the matching tracker task.
---

# create-ticket

Every step of this workflow lives in one file, shared by all agents:
**`dispatch/workflow/create-ticket.md`**. Read it now and follow it exactly — this file
carries no steps of its own.

Wherever it says `<ARGS>`, substitute the arguments you were
invoked with (`short description + where it came from`).
