---
name: develop
description: Implement a ticket — precondition checks, status move, code with tests, gates green, then hand off to /code-review in a fresh session.
---

# develop

Every step of this workflow lives in one file, shared by all agents:
**`dispatch/workflow/develop.md`**. Read it now and follow it exactly — this file
carries no steps of its own.

Wherever it says `<ARGS>`, substitute the arguments you were
invoked with (`<ticket id>`).
