---
name: test-plan
description: Write the manual test plan for a code-complete ticket — only what automation can't cover — set open_tests, freeze the contract zone, move to review.
---

# test-plan

Every step of this workflow lives in one file, shared by all agents:
**`dispatch/workflow/test-plan.md`**. Read it now and follow it exactly — this file
carries no steps of its own.

Wherever it says `<ARGS>`, substitute the arguments you were
invoked with (`<ticket id>`).
